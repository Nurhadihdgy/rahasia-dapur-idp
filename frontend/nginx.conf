server {
    listen 80;
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Baris "Sakti" di bawah ini untuk menangani routing React/Vue
        try_files $uri $uri/ /index.html;
    }
}